#!/usr/bin/env node
// TODO(impl): CLI (commander) with commands:
//   list      — show accounts (mark current)
//   status    — account count + rotation position
//   add -k <key> -l <label>  — add an account
//   remove <number> — remove by 1-based number
//   quota     — per-account live quota (rolling/weekly/monthly percent + resetsAt)
// Build as CommonJS (dist/cli.cjs) so it runs under plain Node (no Bun needed).

throw new Error("not implemented")
